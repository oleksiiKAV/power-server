const errorTimeMessages = {
  "string.base": "The time must be a string.",
  "any.required": "The time field is required.",
  "string.empty": "The time field must be filled",
};

module.exports = errorTimeMessages;
