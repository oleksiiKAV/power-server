const errorЕxerciseMessages = {
  "string.base": "The exercise must be a string.",
  "any.required": "The exercise field is required.",
  "string.empty": "The exercise  must not be empty.",
};

module.exports = errorЕxerciseMessages;
