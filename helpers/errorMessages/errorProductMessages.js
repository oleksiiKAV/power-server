const errorProductMessages = {
  "string.base": "The product must be a string.",
  "any.required": "The product field is required.",
  "string.empty": "The product must not be empty.",
};

module.exports = errorProductMessages;
